# https://docs.aws.amazon.com/sdkref/latest/guide/file-format.html
# Consider moving config and credentials files to ~/.config/aws in the future by setting
# AWS_CONFIG_FILE and AWS_SHARED_CREDENTIALS_FILE environment variables. This can not be
# done in a clean way for the moment as AWS CLI cache and alias file are required to be
# under ~/.aws

[default]
cli_auto_prompt = on-partial
cli_binary_format = base64
cli_history =
output = json
parameter_validation = true
region = us-west-2
retry_mode = standard
