# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) across all repositories.

## General Preferences

- Respond in English
- After exiting plan mode, save the final plan to `.claude/plans/` with a descriptive filename (e.g., `add-auth-flow.md`). Create the directory if it doesn't exist.

## Code Style

- Always use 4-space indentation
